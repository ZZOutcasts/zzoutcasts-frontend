import type { NextPage } from 'next'
import { CreateProjectFormContainer } from '@features/createProject/components'

const MyProjects: NextPage = () => {
  return <CreateProjectFormContainer />
}
export default MyProjects
