import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  iconContainer: {
    width: '100%',
    height: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: '50px'
  }
}))
